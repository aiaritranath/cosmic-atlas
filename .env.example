# Optional production database. Use a hosted PostgreSQL provider such as Neon/Supabase.
DATABASE_URL=
# Optional future API secrets. NEVER expose private keys through NEXT_PUBLIC_* variables.
NASA_API_KEY=
